import { CategoryType } from "../../types/CategoryType";

const BASE_URL = 'http://plug.plug';

export const getCheaper = async (category: CategoryType) => {
	const result = {
		href: 'https://google.com',
		price: 35,
	}

	return result;
}